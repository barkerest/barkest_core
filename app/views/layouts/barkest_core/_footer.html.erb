<div class="container">
  <footer>
    <%= render 'layouts/footer_copyright' %>
    <nav>
      <ul>
        <%= render_for_namespace 'menu_footer' %>
        <% BarkestCore.send(:footer_menu_registry).reverse.each do |partial| %>
            <%= render partial rescue nil %>
        <% end %>
      </ul>
    </nav>
  </footer>
  <%= debug(params) if Rails.env.development? %>
</div>
